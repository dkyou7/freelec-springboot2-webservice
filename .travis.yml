language: java
jdk:
  - openjdk8

branches:
  only:
    - master



# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: "./gradlew clean build"

# 빌드 전에 접근 권한 주기
before_install:
  - chomod +x gradlew


# CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - dkyou7@naver.com